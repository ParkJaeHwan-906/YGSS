apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ygss-infra

resources:
  - ../../base

patchesStrategicMerge:
  - patch-backend.yml
  - patch-ingress.yml

images:
  - name: ygss-backend
    newTag: refactor-${BUILD_NUMBER}
  - name: ygss-ai
    newTag: refactor-${BUILD_NUMBER}

commonLabels:
  env: infra-refactor

replicas:
  - name: backend
    count: 1