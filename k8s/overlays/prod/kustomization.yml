apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ygss-prod

resources:
  - ../../base

patchesStrategicMerge:
  - patch-backend.yml
  - patch-ingress.yml

images:
  - name: ygss-backend
    newTag: prod-latest
  - name: ygss-ai
    newTag: prod-latest

commonLabels:
  env: production

replicas:
  - name: backend
    count: 1
  - name: ai
    count: 1